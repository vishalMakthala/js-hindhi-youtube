const name = "hitesh"
const repoCount = 50
console.log(name + repoCount +" VAlue");
console.log(`Hello my ${name} and my repo count is ${repoCount}`);
//this is the morden way of syntax this called sting interpolatin
// the advantages of writing like is this can be modified on the go
// like make name upper case by adding syntax(.toUppercase)  